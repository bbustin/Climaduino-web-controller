# open shell with 'python manage.py shell'

# Change all programs on Monday-Friday @ 8AM 76F to 6:30AM
from settings.models import Program
for program in Program.objects.filter(temperature=76, day__range=(0,4), time="8:00:00"):
	program.time = "6:30:00"
	program.save()